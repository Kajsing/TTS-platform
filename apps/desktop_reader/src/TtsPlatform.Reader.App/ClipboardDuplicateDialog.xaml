<Window x:Class="TtsPlatform.Reader.App.ClipboardDuplicateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text already exists" Width="480" Height="210"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ShowInTaskbar="False"
        AutomationProperties.Name="Duplicate clipboard text">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="Text already exists" FontSize="20" FontWeight="SemiBold" />
    <TextBlock Grid.Row="1" Margin="0,10,0,14" TextWrapping="Wrap"
               Text="Identical clipboard text is already saved. Open the existing document, create another copy, or cancel." />
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Open existing" Click="OpenExisting_Click" IsDefault="True" />
      <Button Content="Create anyway" Click="CreateAnyway_Click" Margin="8,0,0,0" />
      <Button Content="Cancel" Click="Cancel_Click" IsCancel="True" Margin="8,0,0,0" />
    </StackPanel>
  </Grid>
</Window>
