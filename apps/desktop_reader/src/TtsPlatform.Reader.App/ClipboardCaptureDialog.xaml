<Window x:Class="TtsPlatform.Reader.App.ClipboardCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New clipboard text" Width="640" Height="380" MinWidth="540" MinHeight="320"
        WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ShowInTaskbar="True"
        ShowActivated="True" Topmost="True" ResizeMode="CanResizeWithGrip"
        AutomationProperties.Name="Clipboard capture actions">
  <Grid Background="{StaticResource AppBackgroundBrush}">
    <Border Style="{StaticResource CardStyle}" Margin="14" Padding="18">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel>
          <Border DockPanel.Dock="Left" Width="38" Height="38" CornerRadius="19"
                  Background="#E4F1F1" Margin="0,0,11,0">
            <Path Data="{StaticResource PlusGeometry}" Width="17" Height="17" Stretch="Uniform"
                  Fill="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="New clipboard text" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Text="Choose what Reader should do with the copied text." Foreground="{StaticResource TextMutedBrush}" />
          </StackPanel>
        </DockPanel>
        <TextBlock Grid.Row="1" x:Name="SourceText" Margin="0,11,0,8"
                   Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" />
        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Background="{StaticResource SurfaceMutedBrush}" CornerRadius="7" Padding="13">
          <ScrollViewer VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="PreviewText" TextWrapping="Wrap" FontSize="15" LineHeight="22"
                       AutomationProperties.Name="Clipboard preview" />
          </ScrollViewer>
        </Border>
        <WrapPanel Grid.Row="3" Margin="0,12,0,0">
          <Button Content="Add to open article" Click="Append_Click" IsDefault="True"
                  Style="{StaticResource PrimaryButtonStyle}" />
          <Button Content="Create new article" Click="Create_Click" />
          <Button Content="Save to Inbox" Click="Inbox_Click" />
          <Button Content="Read now" Click="ReadNow_Click" />
          <Button Content="Ignore" Click="Ignore_Click" />
        </WrapPanel>
        <Button Grid.Row="4" x:Name="AlwaysIgnoreButton" Content="Always ignore clipboard changes from this app"
                HorizontalAlignment="Left" Margin="3,5,3,0" Click="AlwaysIgnore_Click" />
      </Grid>
    </Border>
  </Grid>
</Window>
