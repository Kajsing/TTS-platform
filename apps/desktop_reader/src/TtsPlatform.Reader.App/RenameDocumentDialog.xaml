<Window x:Class="TtsPlatform.Reader.App.RenameDocumentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename document" Width="520" Height="190" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ShowInTaskbar="False"
        AutomationProperties.Name="Rename document">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="Document title" FontSize="20" FontWeight="SemiBold" />
    <TextBox Grid.Row="1" x:Name="TitleTextBox" Margin="0,10,0,0" MaxLength="500"
             AutomationProperties.Name="New document title" />
    <TextBlock Grid.Row="2" x:Name="ValidationText" Foreground="#9A1B1B" Margin="0,6,0,0" />
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="SaveButton" Content="Save title" Click="SaveButton_Click" IsDefault="True" />
      <Button Content="Cancel" IsCancel="True" Margin="8,0,0,0" />
    </StackPanel>
  </Grid>
</Window>
